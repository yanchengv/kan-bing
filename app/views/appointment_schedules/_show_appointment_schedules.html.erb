
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h4 class="cancel">预约安排<%#=t('.cancel schedules') %></h4>
      </div>
      <%= form_for 'app_sch', remote: true, :url=>{:action=>'cancelthisweekschedule'},:id => "cacelform" , :html => { :method => :post } do |f| %>
          <div class="modal-body" style='height:120px;'>
            <p><%#= label_tag  '您确定要取消这次预约计划吗？' %></p>
            <!--<div class="form-group">-->
            <!--<div class="col-sm-6">-->
            <!--<%#= radio_button_tag(:cacelmode, "allweek" , true ) %>取消所有周的本次安排<%#= label_tag(t('.cancel all')) %>-->
            <!--</div>-->
            <!--<div class="col-sm-6">-->
            <!--<%#= radio_button_tag(:cacelmode, "cancelthisweek") %>取消本周或下周的本次安排<%#= label_tag(t('.cancel this')) %>-->
            <!--</div>-->
            <!--</div>-->
            <table class="table font-12" style='border: solid #bbbbbb 1px; '>
              <tr>
                <th>计划总预约人数</th>
                <th>已预约人数</th>
                <th>剩余可预约人数</th>
                <th>操作</th>
              </tr>
              <tr>
                <td id='total_count'><%=@app_sch.avalailbecount %></td>
                <td id='app_count'><%= (@app_sch.avalailbecount - @app_sch.remaining_num) %></td>
                <td id='remaining_count'><%=@app_sch.remaining_num %></td>
                <td><a href="/appointment_schedules/<%=@app_sch.id %>"  id="tmpbtn" class="btn btn-primary btn-xs" data-method="delete" rel="nofollow">取消预约安排<%#=t('users.settings.submit') %></a></td>
              </tr>
            </table>
            <%#= hidden_field_tag(:cancelappscheduleId, "") %>
            <%#=  hidden_field_tag(:diagnoseId, "") %>
            <%#=  hidden_field_tag(:doctorId, params[:doctor_id])%>
          </div>
          <div class="modal-footer">
            <div class="form-group">
              <div class="col-sm-8">
              </div>
              <div class="col-sm-4">
                <button class="btn btn-default" data-dismiss="modal" aria-hidden="true">关闭<%#=t('.cancel') %></button>
                <!--<a href="#"   id="tmpbtn" class="btn btn-primary" data-method="delete" rel="nofollow">取消预约安排<%#=t('users.settings.submit') %></a>-->
              </div>
            </div>
          </div>
      <% end %>
    </div>
  </div>